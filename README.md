# ***FB AUTO BOOST***

***By: @Porio_ke***

---

### *Setup & Usage Instructions* 🛠️

* ***Add your card first*** — *Open Meta Pay or Ads Manager billing in Firefox. Verify the card is active and has available balance. The script scans for `payment`, `billing`, `insufficient`, and `fund` keywords; missing cards trigger `❌ Ad Failed: Payment method issue` before any submit.* 💳
* ***Choose your publish mode*** — *Open the bookmarklet code and locate `autoPublish` inside `CFG`.* ⚙️
* *`true` submits the Boost button the instant Facebook enables it (`aria-disabled false`).* ⚡
* *`false` stops after filling and shows a blue footer button (`⚡ Click to Publish / Boost`) so you confirm budget ($7), duration (2), country (BD), goal (MESSAGES), and message (BY PORIOTKE) before committing spend.* 🎯


* ***Manual is safer for first run*** — *With `false`, you watch the black panel (`#1c1e21`) fill green rows (`✅ Budget set`, `✅ Goal set`) and verify the modal fields match your intent. Once you trust the flow, switch `autoPublish` back to `true`.* 🛡️
* ***Confirm Firefox bookmark*** — *Navigate to `facebook.com`, edit the bookmark URL to hold the stripped `javascript:(function()...` block, then open it from Bookmarks (`⋯` → `Bookmarks` → `FB Boost`).* 🔖
* ***Watch the panel*** — *🚀 opens the Boost dialog. ⚠️ warns if IDs are missing. ❌ flags errors. Only after green rows stack should you submit—either automatically or by tapping the footer button.* 👁️

### Telegram 
https:t.me/Poriot_ke

### Whatsapp
https://wa.me/+254717702563
